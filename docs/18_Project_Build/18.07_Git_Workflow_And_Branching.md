# Git Workflow

Main branch must remain stable.

Example:

main
|
├── feature/inventory
├── feature/accounting
├── feature/calendar
└── feature/mobile-ui

---

## Commit Rules

Commits must be small and descriptive.

Examples:

feat(inventory): add stock consumption terminal

fix(calendar): resolve tablet navigation issue

docs(accounting): add journal workflow documentation