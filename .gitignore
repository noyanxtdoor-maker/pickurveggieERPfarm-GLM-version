node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Code-knowledge tool output (generated, not source)
.codegraph/
graphify-out/

# Browser-probe scratch artifacts (session-local, not source)
.tmp_capture/

# Vercel local project link (session-local, not source — Vercel dashboard is the truth)
.vercel/

# Supabase Studio SQL Editor auto-saves "private queries" here (exploration scratch, not source)
supabase/snippets/

# Database backups (pg_dump output — contains schema + possibly user data; never commit)
backups/

# Hermes agent working scratch (plans, etc. — session-local, not source)
.hermes/
